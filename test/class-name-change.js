import chai, { assert, expect } from 'chai';
import classNameChange from '../src/class-name-change';

describe('#classNameChange', () => {
  it('should poll for a thing', () => {
    assert.equal('shannonr', 'shannonr'); // 'good person');
  });
});
