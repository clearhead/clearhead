import chai, { assert, expect } from 'chai';
import curry from '../src/google-analytics';

describe('#googleAnalytics()', () => {

  it('should googleAnalytics a thing', () => {
    assert.equal('tomf', 'tomf'); // 'good person');
  });

});
