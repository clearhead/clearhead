import chai, { assert, expect } from 'chai';
import getParam from '../src/get-param';

describe('#getParam()', () => {

  it('should return the value of a key given a URL string with query parameters', () => {
    
  });

});
