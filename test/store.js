import chai, { assert, expect } from 'chai';
import curry from '../src/store';

describe('#store()', () => {

  it('should store a thing', () => {
    assert.equal('tomf', 'tomf'); // 'good person');
  });

});
