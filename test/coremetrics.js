import chai, { assert, expect } from 'chai';
import curry from '../src/coremetrics';

describe('#coremetrics()', () => {

  it('should coremetrics a thing', () => {
    assert.equal('tomf', 'good person');
  });

});
