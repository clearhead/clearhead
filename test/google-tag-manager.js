import chai, { assert, expect } from 'chai';
import curry from '../src/google-tag-manager';

describe('#googleTagManager()', () => {

  it('should googleTagManager a thing', () => {
    assert.equal('tomf', 'tomf'); // 'good person');
  });

});
