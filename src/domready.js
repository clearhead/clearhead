/**
 * @desc domready() runs a function on domready
 *
 * @param {Function} fn - the function to run on domready
 *
 * @return {undefined}
 */

export default require('../bower_components/domready/ready.min.js');
